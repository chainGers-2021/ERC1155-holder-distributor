{
  "name": "erc1155-nft-token-and-holder",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "compile": "npx truffle compile",
    "test": "npx truffle test",
    "migrate": "npx truffle migrate --reset --network rinkeby",
    "verify": "npx truffle run verify Token --network rinkeby --license MIT"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.4.1",
    "@truffle/hdwallet-provider": "1.0.40",
    "dotenv": "^8.2.0",
    "truffle-plugin-verify": "^0.5.7"
  }
}
